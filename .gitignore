# Build outputs (generated by build.sh / GitHub Actions)
cliproxy-thinking-mask.so
cliproxy-thinking-mask.h
*.dll
*.dylib
*.exe

# Go build/test artifacts
*.test
*.out

# Local patch export (the tracked source of truth is _cpa-overlay/)
cpa-patch/

# Local Downloads (Actions artifacts pulled for verification)
.downloads/